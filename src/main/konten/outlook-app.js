'use strict';

// Anwendungs-ID der gemeinsamen Julia-App bei Microsoft. Ein öffentlicher
// Client hat kein Geheimnis – die ID steckt in jeder Desktop-App offen drin.
// Ist sie gesetzt, reicht zum Verbinden ein Klick; eine eigene ID aus einer
// eigenen App-Registrierung bleibt möglich.
module.exports = { EINGEBAUTE_ID: '' };
